# Screenshots

Real screenshots of DashWoo: the settings screen, the account design screen, the account
panel with its menu card and content card, a custom endpoint page with its own content
box, and the four-step setup wizard - in English and in Persian.

They are produced by `tools/preview/` from the shipped views and the shipped stylesheets,
so they show what the plugin actually renders. The public project page shows these files.
